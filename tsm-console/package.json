{
  "name": "tsm-console",
  "version": "0.2.0",
  "private": true,
  "description": "Tri-State Systems Manager — Infinite Orchestration Console",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:proxy": "node server/token-proxy.mjs",
    "dev:all": "node server/token-proxy.mjs & vite",
    "build": "vite build",
    "preview": "vite preview",
    "proxy": "node server/token-proxy.mjs",
    "ingest": "node -e \"import('./server/ingestion/workers.mjs').then(m => m.runHydrologicBatch().then(r => console.log(JSON.stringify(r,null,2))))\"",
    "buf:lint": "buf lint",
    "buf:gen": "buf generate",
    "buf:breaking": "buf breaking --against \".git#branch=main\"",
    "proto:all": "buf lint && buf generate",
    "postinstall": "node scripts/postinstall-check.mjs",
    "test": "node --test tests/geodetic-pipeline.test.mjs",
    "test:geodetic": "node --test tests/geodetic-pipeline.test.mjs",
    "ingest:ras": "node scripts/ras-geotiff-ingest.mjs",
    "check:parse": "node scripts/parse-check.mjs",
    "check:type": "npx tsc --noEmit",
    "test:cinematic": "node --test tests/cinematic-contracts.test.ts 2>/dev/null || node --test tests/*.test.mjs",
    "scan:loma": "node scripts/loma-candidate-scan.mjs",
    "etl:posey": "node server/ingestion/posey-parcel-nfhl-etl.mjs",
    "resync": "npm install --no-audit --no-fund && node scripts/postinstall-check.mjs && node scripts/parse-check.mjs",
    "ci": "npm run check:parse && npx tsc --noEmit && npm run build"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router": "^7.0.0",
    "three": "^0.170.0",
    "@react-three/fiber": "^9.0.0",
    "@react-three/drei": "^10.0.0",
    "maplibre-gl": "^4.7.1",
    "lucide-react": "^0.468.0",
    "@tanstack/react-virtual": "^3.11.0",
    "@bufbuild/protobuf": "^2.2.0",
    "h3-js": "^4.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/three": "^0.170.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "@bufbuild/buf": "^1.47.0",
    "@bufbuild/protoc-gen-es": "^2.2.0",
    "@webgpu/types": "^0.1.60"
  },
  "engines": {
    "node": ">=20"
  }
}
