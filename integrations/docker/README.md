# Docker Capability Adapter

This directory defines the TSM-owned boundary for ephemeral containerized integration tests. External Docker CI examples are translated into TSM-specific service contracts rather than copied into production.
