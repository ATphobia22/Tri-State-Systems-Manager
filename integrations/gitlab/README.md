# GitLab Capability Adapter

GitLab is not a prerequisite for TSM application CI. This adapter records self-hosted runner, registry, and infrastructure patterns that can be used when TSM operates dedicated infrastructure.

The canonical application gate remains GitHub Actions. Any future GitLab mirror must consume the same TSM validation contracts and cannot become a second source of CI truth.
