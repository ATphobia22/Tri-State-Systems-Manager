# Vagrant Infrastructure Patterns

Vagrant remains an optional local reproducibility/reference layer. It is not required for GitHub-hosted TSM CI and does not provision production infrastructure by default.
