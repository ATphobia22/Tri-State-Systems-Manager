# Docker Integration Infrastructure

Docker is used for deterministic, ephemeral CI integration environments and service-dependency testing.

The legacy `docker-ci-tool-stack` repository is a reference for topology and service orchestration patterns only. Its containers are not imported wholesale and are not treated as production images.
