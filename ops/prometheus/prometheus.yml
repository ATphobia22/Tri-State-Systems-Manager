global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

rule_files:
  - /config/rules/tsm-hydrologic-alerts.yml

scrape_configs:
  - job_name: tsm-telemetry
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:8788
